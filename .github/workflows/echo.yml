name: echo

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        run: sleep 20

  after-echo:
    runs-on: ubuntu-latest
    needs: echo
    steps:
      - name: checkout
        run: sleep 20
