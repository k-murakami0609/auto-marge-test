name: echo

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopenedw

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        run: sleep 20

  after-echo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        run: sleep 20
