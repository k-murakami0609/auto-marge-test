name: echo

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        run: sleep 40
